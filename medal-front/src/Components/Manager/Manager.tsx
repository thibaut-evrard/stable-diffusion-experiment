import {Canvas} from '@react-three/fiber';
import Scene from '../Scene/Scene';

const Manager = () => {
    return (
        <Canvas>
            <Scene />
        </Canvas>
    );
};

export default Manager;
